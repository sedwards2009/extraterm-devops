{
  "name": "list-bucket",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && cp src/*.html src/*.xml dist/",
    "serve": "http-server -p 8080 dist/",
    "watch": "watch 'yarn run build' src/"
  },
  "author": "Simon Edwards",
  "license": "MIT",
  "devDependencies": {
    "http-server": "^0.11.1",
    "typescript": "^3.4.2",
    "watch": "^1.0.2"
  }
}
